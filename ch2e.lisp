; Write a version of generate that uses cond but avoids calling rewrites twice.

; Write a version of generate that explicitly differentiates between terminal symbols (those with no rewrite rules)
; and nonterminal symbols.

; Write a trivial grammar for some other language. This can be a natural language other than English,
; or perhaps a subset of a computer language.

; One way of describing combine-all is that it calculates the cross-product of the function append on the argument lists.
; Write the higher-order function cross-product, and define combine-all in terms of it.
; The moral is to make your code as general as possible, because you never know what you may want to do with it next.
